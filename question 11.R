# Create the vector
number <- c(5, 12, 8, 23, 15, 7, 19)

# Perform calculations
mean_val   <- mean(number)     # Mean
median_val <- median(number)   # Median
sum_val    <- sum(number)      # Sum
min_val    <- min(number)      # Minimum
max_val    <- max(number)      # Maximum

# Display results
mean_val
median_val
sum_val
min_val
max_val
